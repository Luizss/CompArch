#include <stdio.h>
#include <stdlib.h>

int main() {
  // Finding the largest number
  for (int i = 1; i < n; ++i) {
    if (*data < *(data + i)) {
      *data = *(data + i);
    }
  }

  return 0;
}
